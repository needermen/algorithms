def solution(A):
    # write your code in Python 3.6
    N = len(A)
    arr = [None] * N
    for a in A:
        if a > 0 and a <= N:
            arr[a-1]='yes'
    for i,a in enumerate(arr):
        if a == None:
            return i + 1
    return N+1